// Re-export from new location for backward compatibility
export {
  formatTime,
  normalizeText,
  normalizeSearch,
  escapeHtml,
  getInitials,
  isV2RayMode,
  ALLOWED_LOG_TAGS,
} from "../utils/helpers";
